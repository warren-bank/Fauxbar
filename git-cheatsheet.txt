# --------------------------------------------------------------------------------------------------

git checkout -f master
git checkout -b "pulls/01-fix-POST-data-encoding"
git add --all .
git commit -m "fix POST data encoding"
git push -u origin "pulls/01-fix-POST-data-encoding"

# https://github.com/ChrisNZL/Fauxbar/compare/master...warren-bank:pulls/01-fix-POST-data-encoding
# --------------------------------------------------------------------------------------------------

git checkout -f master
git checkout -b "pulls/02-reset-search-engines"
git add --all .
git commit -m "reset of options should include removal of non-default search engines"
git push -u origin "pulls/02-reset-search-engines"

git add --all .
git commit -m "add a new button to trigger this action"
git push -u origin "pulls/02-reset-search-engines"

git add --all .
git commit -m "add handler to update list of search engines when the set is empty"
git push -u origin "pulls/02-reset-search-engines"

# https://github.com/ChrisNZL/Fauxbar/compare/master...warren-bank:pulls/02-reset-search-engines
# --------------------------------------------------------------------------------------------------

git checkout --orphan "pulls/notes"
git rm -rf .
git add --all .
git commit -m 'git cheatsheet'
git push -u origin "pulls/notes"

# --------------------------------------------------------------------------------------------------

git checkout -f master
git checkout -b "pulls/merge"
git merge --no-edit "pulls/01-fix-POST-data-encoding"
git merge --no-edit "pulls/02-reset-search-engines"
git push -u origin "pulls/merge"
git tag "v1.7.4-merge-001"
git push --tags

# https://github.com/ChrisNZL/Fauxbar/compare/master...warren-bank:pulls/merge
# --------------------------------------------------------------------------------------------------

git checkout -f "pulls/notes"
git add --all .
git commit -m 'git cheatsheet'
git push -u origin "pulls/notes"

git add --all .
git commit -m 'data backup: options, search engines'
git push -u origin "pulls/notes"

# --------------------------------------------------------------------------------------------------

git checkout -f master
git checkout -b "pulls/03-detect-static-search-engine-urls"
git add --all .
git commit -m "search URL without dynamic placeholder is static bookmark"
git push -u origin "pulls/03-detect-static-search-engine-urls"

# https://github.com/ChrisNZL/Fauxbar/compare/master...warren-bank:pulls/03-detect-static-search-engine-urls
# --------------------------------------------------------------------------------------------------

git checkout -f "pulls/merge"
git merge --no-edit "pulls/03-detect-static-search-engine-urls"
git push -u origin "pulls/merge"
git tag "v1.7.4-merge-002"
git push --tags

# https://github.com/ChrisNZL/Fauxbar/compare/master...warren-bank:pulls/merge
# --------------------------------------------------------------------------------------------------

git checkout -f "pulls/notes"
git add --all .
git commit -m 'git cheatsheet'
git push -u origin "pulls/notes"

git add --all .
git commit -m 'data backup: options, search engines'
git push -u origin "pulls/notes"

# --------------------------------------------------------------------------------------------------
